# Local-dev Postgres + Redis for SkyMatch. Production uses Supabase Postgres + Railway
# Redis; this gives isolated, disposable local services matching the prod major versions.
# Not used in prod.
#
#   docker compose up -d        # start Postgres + Redis
#   alembic upgrade head        # create the schema (see CLAUDE.md "Running the project")
#   docker compose down         # stop  (add -v to also wipe the data volume)
#
# Redis is OPTIONAL for local dev: with no REDIS_URL the guard + Sonnet governor run
# in-process (single-worker behaviour). Set REDIS_URL=redis://localhost:6379/0 to exercise
# the distributed path locally.
services:
  db:
    image: postgres:16
    container_name: skymatch-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      # Still `pathfinder` after the SkyMatch rename, deliberately: renaming the database
      # would orphan the existing `pathfinder-pgdata` volume (a rename creates a fresh empty
      # one), forcing a re-run of `alembic upgrade head` plus a full ingestion pass. It must
      # also keep matching the healthcheck's -d flag and the DSNs in .env.example.
      POSTGRES_DB: pathfinder
    ports:
      - "5432:5432"
    volumes:
      - pathfinder-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d pathfinder"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7
    container_name: skymatch-redis
    command: ["redis-server", "--save", "", "--appendonly", "no"]  # ephemeral; guard state is recreatable
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pathfinder-pgdata:
