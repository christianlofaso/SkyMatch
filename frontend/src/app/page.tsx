import { Landing } from "@/components/landing/Landing";

// The landing IS the home page and carries the real run input (ProfileInput → /run/stream).
export default function Home() {
  return <Landing />;
}
